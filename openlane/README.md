# OpenLane 2 configuration for specific hardening of this design as a macro

As a macro, this can be placed in an analog template instead. For that we need specific control over area, pins, and obstructions.

